{
  "name": "metric-studio",
  "version": "0.4.0",
  "description": "Data planning / metric governance workspace: Metric Master, Structural Hierarchy, TT/GD scenario bindings, dimensions and dependency graph.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "deploy": "npx wrangler deploy",
    "preview": "npx wrangler dev"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
